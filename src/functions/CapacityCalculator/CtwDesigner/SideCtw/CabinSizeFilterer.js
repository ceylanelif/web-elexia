import cabinSize from "./CabinSize";

export default function SideCabinSizeFilterer(ctw,liftInfo,constants,selectedOptions) {

    const allSizes=cabinSize(ctw,liftInfo,constants,selectedOptions);
    const results=[];


return allSizes;    
}